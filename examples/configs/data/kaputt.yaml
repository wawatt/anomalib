class_path: anomalib.data.Kaputt
init_args:
  root: ./datasets/kaputt
  category: book_other
  train_batch_size: 32
  eval_batch_size: 32
  num_workers: 8
  image_type: image # "image" for full images, "crop" for cropped regions
  image_mode: query_only # query_only | query_and_reference | reference_only
  test_split_mode: from_dir
  test_split_ratio: 0.2
  val_split_mode: from_dir # Kaputt has native validation split
  val_split_ratio: 0.5
  seed: null
